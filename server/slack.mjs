// Only an explicit owner action sends a status notification. No article text or private content is sent.
export async function notifySlack(env){if(!env.SLACK_WEBHOOK_URL)throw Error('Slack not configured');const u=new URL(env.SLACK_WEBHOOK_URL);if(u.protocol!=='https:'||u.hostname!=='hooks.slack.com'||!u.pathname.startsWith('/services/'))throw Error('invalid Slack webhook');const r=await fetch(u,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:'htw0702.com Studio：內容管理後台連線測試成功。'}),signal:AbortSignal.timeout(10000)});if(!r.ok)throw Error('Slack unavailable');return {ok:true};}
